PORT=5000
NODE_ENV=production
# PostgreSQL connection string from Neon (or any Postgres provider)
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"
# Optional Gemini API key for live AI LLM intent extraction. If omitted, Demo Fallback engine runs automatically!
GEMINI_API_KEY=""
